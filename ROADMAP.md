# ROADMAP

おぼえトレ（旧: 逆復唱トレーニング）の今後の開発候補・バックログ。優先度や着手判断は都度相談の上で決める。完了した項目は[CHANGELOG.md](CHANGELOG.md)に移す。

## 進行中

- なし。公開Webアプリ実機操作＋リポジトリの総合監査（2026-08-11実施）で発見した最優先バグ4件（処理記憶モードの履歴消失・実績誤解除・「9種類」表記の残存・回帰テスト不足）の修正が2026-08-11に完了

## 候補（未着手）

プロダクト監査（現状分析→問題発見→優先順位付け）に基づく候補。実装を依頼された際にここから拾う、あるいは新規探索して追加する。

優先度: 新機能 > UX/UI改善 > バグ修正 > パフォーマンス > アクセシビリティ > テスト > コード品質

- [ ] SetSummaryで複数の演出バナー（🍀ラッキー/🎉レベルアップ/自己ベスト/実績解除/インストール促進/今日の目標）が同時に表示された際に縦に積み上がり煩雑になる問題。発生頻度は低いが、優先順位付け（最も重要な1〜2件のみ強調し、残りはまとめて簡潔表示する等）を検討する価値がある
- [ ] 個別選択モード画面（ModeSelectScreen.tsx）のモードカードに、直近の正答率やレベルなど「今の自分の状態」をひと目で分かる情報を追加する案（現状はアイコン・タイトル・説明文のみ）
- [ ] TopScreen.tsx/DailyMissionScreen.tsxが毎レンダーごとに`loadHistory`/`loadSettings`/`loadMissionCompletions`/`loadDailyChallengeCompletions`など複数のlocalStorage読み込みを行っており、将来的にレンダー回数が増えた場合のパフォーマンスへの影響を計測・検討する
- [ ] Nバック/デュアルNバックの結果画面で、折りたたんだ内訳の代わりに「どの区間で間違えやすかったか」等の集約的なフィードバックを追加する案（現状は正答率のみ）
- [ ] エラーバウンダリ（ErrorBoundary.tsx）表示時のUXを実際に確認し、再読み込み以外の導線（トップに戻る等）が必要か検討する
- [ ] ホーム画面から撤去した旧「今日のミッション」（missions.ts、日替わり固定9種）「本日のお題」（dailyChallenge.ts）「7日間チャレンジ」（program.ts）のコードを、今後も使わないと判断できた時点で削除する（現時点ではユーザー判断により「コードは残す」扱い。データ・XP計算には引き続き影響する）
- [ ] 新設の「今日のミッション」画面のターゲット選定を、正答率だけでなく直近の挑戦頻度なども加味してより精緻化する案（現状は`getWeakestAreas`による正答率ベースの選定のみ）
- [ ] すうじモードで、数字表示中（showingフェーズ）に決定ボタン相当の位置を連打すると、answeringフェーズへの切替直後に空回答のまま自動submitされてしまう現象を実機で確認（2026-08-11の監査で再現）。フェーズ切替直後の入力を一定時間無視するデバウンス等の対応を検討
- [ ] 回答中の離脱確認（`src/lib/confirmExit.ts`）がブラウザネイティブの`window.confirm()`のままで、作り込まれたカスタムUIの中で唯一トーンが浮いている。既存のモーダルUIパターンを流用したカスタム確認ダイアログへの置き換えを検討
- [ ] `RandomGameScreen.tsx`（698行）に、すうじ/空間/変化検出/音・色4モード分の入力ハンドラが単体GameScreenと別実装で存在し重複している。単体モード側の仕様変更が反映されない将来のバグの温床になりうるため、小さな共有フックへの段階的な切り出しを検討
- [ ] `api/push/subscribe.ts`の購読エンドポイント（`subscription.endpoint`）が文字列型チェックのみでURL形式・ドメインの検証が無い。`new URL()`でのパース＋既知のプッシュサービスドメインのホワイトリスト検証の追加を検討（実害は限定的な軽微リスク）
- [ ] 複数モードを跨ぐ「今日のプログラム」（弱点カテゴリから複数モードを自動編成する複合カリキュラム）の追加。現状の今日のミッションv2は単一モード×3セットに閉じている
- [ ] 演出（効果音・ハプティクス・XPアニメーション）を排した月1回の「純粋計測モード」の追加。現行の自己ベンチマークは通常プレイの前半/後半比較のため、ゲームへの習熟効果とワーキングメモリ向上効果を分離できていない

以下は費用対効果・実施可否・設計哲学との整合を検討した結果、見送りと判断し候補から除外した（2026-08-10）:
- ランダムモードの対象をNバック系（連続試行方式）まで拡張する案。試行位置が内部stateから導出され外部の「単発質問→回答」型の状態機に統合しづらいため見送り（なお、ことばモードは2026-08-11に自己完結した専用コンポーネント`RandomWordRound.tsx`として統合済み。詳細は完了欄を参照）
- 「ワーキングメモリの伸び」を他ユーザーとの相対比較に拡張する案。匿名集計用のバックエンド・データ集計基盤の新設が必要で、本アプリが一貫して維持している「バックエンドを持たないSPA」という設計哲学（[CLAUDE.md](CLAUDE.md)のOverview参照）と正面から矛盾するため
- Vercel Proプランへの移行（プッシュ通知の送信時刻をユーザーごとに選べるようにする案）。費用が発生する意思決定のためユーザーに確認したところ、見送りと判断された
- PWAマニフェスト（`vite.config.ts`のname/description/shortcuts）と`index.html`のmeta情報（description/OGP/Twitter Card）の英語版対応。ビルド時に固定される静的アセットのため、別ビルド・別デプロイやサーバーサイドでの出し分けなど設計変更が必要でコスト中〜高。効果も「英語UIユーザーがブラウザタブ名やSNSシェアカードで日本語を目にする」という体裁上の問題にとどまるため、費用対効果が見合わないと判断された
- セッション状態の永続化（`src/lib/gameSessionPersistence.ts`）を、ことば/Nバック/デュアルNバックモードにも拡張する案。ことばモードは音声認識のライブストリームが絡み復元の恩恵が小さく、Nバック系は現在の試行位置が`useStepReveal`フック内部のタイマー駆動stateから導出され外部から復元できず、Digit/Spatial/Tone/NBackが共有するこのフック自体の設計変更が必要でコストが高い。ユーザーに説明の上、見送りと判断された
- スクリーンリーダー（NVDA/VoiceOver）実機検証・音声認識/音声合成の実ブラウザ検証・ことばモードのテキスト入力フォールバックのSafari実機確認・Android実機/TWA/Bubblewrapビルドでの実機検証一式。いずれも実機・専用ソフトウェア・ビルド環境が必要でこの開発環境では実施不可なため、候補から除外した（ユーザー自身が必要に応じて実機で確認する）

## 完了

- [x] 要件定義書のCLAUDE.mdへの統合（2026-08-01）
- [x] GitHubリポジトリ連携（[Compass-1024/gyaku-fukushou-app](https://github.com/Compass-1024/gyaku-fukushou-app)、2026-08-01）
- [x] CI（GitHub Actions）導入（2026-08-01）
- [x] E2Eテストの導入（Playwright、`e2e/`配下に主要導線のスモークテスト、CI統合、2026-08-01）
- [x] エラー監視・ロギング方針の実装（`ErrorBoundary`＋グローバルエラーハンドラ、[ERROR_HANDLING.md](ERROR_HANDLING.md)、2026-08-01）
- [x] アクセシビリティ方針の明文化と対応（[ACCESSIBILITY.md](ACCESSIBILITY.md)、aria-label/aria-live/フォーカス管理の見直し、2026-08-01）
- [x] プライバシーポリシーの作成（[PRIVACY.md](PRIVACY.md)＋アプリ内画面＋静的ページ`public/privacy.html`、連絡先メール記載、2026-08-01）
- [x] デプロイ手順書の作成（[DEPLOYMENT.md](DEPLOYMENT.md)、2026-08-01）
- [x] Vercel⇔GitHub のGit連携設定（push時の自動デプロイ化、2026-08-01）
- [x] GitHub Actions の Node.js 20 非推奨警告の解消（`actions/checkout`/`actions/setup-node`をv5系へ更新、2026-08-01）
- [x] `SpeechRecognition`非対応ブラウザ向けの代替回答手段（ことばモードのテキスト入力フォールバック、2026-08-01）
- [x] 結果表示中のEnterキー操作対応（ことば/すうじモード、2026-08-01）
- [x] 学習履歴の一括削除機能（設定画面、2026-08-01）
- [x] 苦手分野判定の精度改善（挑戦回数が十分な項目を優先、2026-08-01）
- [x] SEO/SNSシェア対応（meta description・OGP・Twitter Card、2026-08-01）
- [x] iOSセーフエリアの余白不具合修正（`viewport-fit=cover`追加、2026-08-01）
- [x] ゲーム画面のコード分割によるパフォーマンス改善（React.lazy、2026-08-01）
- [x] Nバック・SetSummaryのキーボード操作拡充（2026-08-01）
- [x] 統計画面に学習カレンダー（GitHub風ヒートマップ）を追加（2026-08-01）
- [x] 学習データのバックアップ（エクスポート/インポート）機能（2026-08-01）
- [x] 結果画面の「結果をシェア」機能（2026-08-01）
- [x] 空間モード（視空間ワーキングメモリ、Corsi Block-Tapping Task参考）の実装（2026-08-01）
- [x] 変化検出モード（視覚パターン記憶、Luck & Vogel 1997参考）の実装（2026-08-01）
- [x] 音・色モード（非言語性の聴覚ワーキングメモリ、Simon型のパッド操作）の実装（2026-08-01）
- [x] 新3モード（空間・変化検出・音/色）のE2Eスモークテスト追加とCLAUDE.md/README.mdへの反映（2026-08-01）
- [x] プッシュ通知リマインダー機能の実装（Vercel Serverless Functions + Redis + Cron、オプトイン、2026-08-01）
- [x] プッシュ通知の本番セットアップ（Upstash for Redis連携・VAPID鍵生成・環境変数設定をVercel CLI経由で実施、2026-08-01）
- [x] プッシュ通知APIの実行時エラー2件を修正・本番Cronエンドポイントの実動作確認（`web-push`のCJS/ESM相互運用エラー、Vercel Node Functionsの実ランタイム形式との不一致。手動トリガーで`{"sent":0,"deleted":0,"errored":0}`を確認、2026-08-01）
- [x] Vercel Hobbyプランのcron制約判明に伴う設計修正（ユーザー別送信時刻→全ユーザー共通固定時刻へ変更、2026-08-01）
- [x] 実機での通知購読を確認（原因はOS側の通知設定によるブロック）。エラーメッセージにOS設定確認を促す文言を追加し、DEPLOYMENT.mdにトラブルシューティング手順を追記（2026-08-01）
- [x] SetSummaryに1日の目標セット数への進捗表示を追加（2026-08-01）
- [x] ストリークフリーズ（連続記録の月2回までの猶予）を追加（2026-08-01）
- [x] 「今日のおすすめ」を間隔反復ベースに強化（正答率＋最終挑戦からの経過日数でスコアリング、2026-08-01）
- [x] PWAマニフェストのショートカット（ことば・すうじ・Nバック・空間の4モード、2026-08-01）
- [x] トップ画面に週間振り返りカードを追加（2026-08-01）
- [x] axe-core（`@axe-core/playwright`）によるWCAG AA自動アクセシビリティ検証をE2Eに導入し、発見したコントラスト比・ラベル不足を修正（2026-08-01）
- [x] SetSummaryのEnterキー操作をE2Eでカバー。あわせて、SetSummary表示中にEnterキーを押すと履歴が二重記録される重大バグを発見・修正（2026-08-01）
- [x] サプライズ演出「🍀 ラッキーデー！」を追加（結果画面表示のたびに12%の確率、実績・統計・レベル判定には無関係、2026-08-01）
- [x] ことばモードの出題プールにフレーズ単位の苦手判定を反映（誤答が多いフレーズほど選ばれやすい重み付き抽選、`src/lib/phraseStats.ts`、2026-08-02）
- [x] バックアップ機能の`VALID_MODES`に新3モード（空間・変化検出・音/色）が未登録で、これらの履歴を含むファイルのインポートが一律拒否されていた不具合を修正（2026-08-02）
- [x] 統計画面に「ことばモード: 苦手なフレーズ」表示を追加（フレーズ単位の正誤履歴から正答率下位5件を抽出、`getWeakestPhrases`、2026-08-02）
- [x] v0.2.0リリース（CHANGELOG.mdの[Unreleased]を切り出し、package.json/package-lock.jsonのバージョンを同期、2026-08-02）
- [x] プライバシーポリシーのバックアップ機能に関する記載漏れを修正（`public/privacy.html`・`PrivacyScreen.tsx`、2026-08-02）
- [x] `e2e/top.spec.ts`を6モード対応に更新（空間・変化検出・音/色モードボタンの表示検証を追加、2026-08-02）
- [x] `src/lib/settings.ts`にユニットテストを追加（これまでテストが1件も無かった、2026-08-02）
- [x] `src/lib/reminder.ts`と`api/_lib/reminder.ts`のコメント差異を解消（実行コードは元々完全一致、2026-08-02）
- [x] アプリ全体のUIを多言語化（日本語/英語）。ことばモードは英語版で非表示。設定画面に言語切り替え、実績・統計画面もことばモード関連を英語版で除外（`src/lib/i18n/`、`src/contexts/LanguageContext.tsx`、2026-08-02）
- [x] 統計画面に「ワーキングメモリの目安」を追加。すうじ（逆から入力）・空間・Nバック・変化検出の各モードを対応する心理学課題（逆唱スパン・視空間スパン・N-back・視覚ワーキングメモリ容量）の一般的な目安レンジと比較し3段階で表示（`src/lib/benchmarks.ts`、医学的診断ではない旨の免責文言つき、2026-08-02）
- [x] 変化検出モードの仕様を「変化あり/なしの2択判定」から「白紙に戻った状態から元々塗りつぶされていたマスを選び直す再生課題」に変更。ベンチマークもCowanのK公式から空間モードと同じ「マスタリー済み最高レベル」方式に変更（`src/lib/pattern.ts`、2026-08-06）
- [x] 順唱モード（Forward Digit Span）を追加。すうじモードのロジック・UIを流用し逆順化のみ行わない単純スパン課題（`src/lib/sequence.ts`、`src/lib/digitAnswer.ts`、2026-08-06）
- [x] Dual N-Backモードを追加。3×3グリッドの位置と8種の合成トーンの音を同時提示し、それぞれ独立に「位置一致」「音一致」を判定する高難度モード。SDTスコアリングをNバックモードと共通化（`src/lib/dualNback.ts`、`scoreMatchTrials`、2026-08-06）
- [x] ランダムモードを追加。すうじ・順唱・空間・変化検出・音/色の5モードから1問ずつシャッフルして出題するミックス練習（`src/lib/random.ts`、2026-08-06）
- [x] 全モード共通の経験値・プレイヤーレベルシステムを追加。正解+10XP・全問正解セット+50XP、レベルごとに必要XPが増加。実績と同じく履歴（＋ミッション達成ログ）から都度動的計算する方式で専用の可変ストアは持たない（`src/lib/xp.ts`、2026-08-06）
- [x] 「今日のミッション」を追加。日付キーから決定的に1件選ばれる日替わりミニタスクで、達成すると+100XP（`src/lib/missions.ts`、`gyaku-fukushou:missionCompletions`、2026-08-06）
- [x] 設定画面に効果音の音量スライダー（0〜100、既定80）を追加。`sound.ts`の共通dry gainノードを一箇所スケーリングする実装で全効果音に一括適用（`AppSettings.sfxVolume`、2026-08-06）
- [x] ホーム画面を縦積みボタンから3×3グリッドのカードUIにリファクタ。プレイヤーLv・XPバー・今日のミッションカードを追加表示（全9モード構成に、2026-08-06）
- [x] 実績を3件追加（📝 順唱上級者・🧠🧠 Dual N-Back上級者・🌠 コンプリート、計17種類、2026-08-06）
- [x] バックアップ（`src/lib/backup.ts`）を`missionCompletions`含む形式に対応（`BACKUP_VERSION: 2`、v1形式との後方互換維持）。あわせて`VALID_MODES`に順唱・Dual N-Back・ランダムの3モードが未登録だった不具合を修正（2026-08-06）
- [x] `public/privacy.html`（静的な法的文書ページ）の英語版`public/privacy-en.html`を追加。アプリ内`PrivacyScreen.tsx`の「プライバシーポリシー全文」リンクは言語設定に応じて出し分ける（2026-08-06）
- [x] プッシュ通知のリマインドメッセージ（`buildReminderMessage`）を購読者のUI言語設定に応じて出し分けるように変更。購読時（`subscribeToPush()`）とセット完了ごとの同期時（`syncPushState()`）の両方で言語を送信し`StoredSubscription.language`に保存（v1レコードは`?? 'ja'`でフォールバック）。あわせて、既に廃止済みの「通知希望時刻」表記がPRIVACY.md/privacy.html/privacy-en.html/i18n辞書に残っていた記載漏れも修正（2026-08-06）
- [x] BGM（背景音楽）を追加。効果音と同じ完全プログラム生成方式（音声ファイル不使用）で、4コードを巡回させるアンビエントパッドを実装。設定画面に効果音とは独立したON/OFF・音量スライダー（既定OFF・50）を追加し、`src/lib/audioContext.ts`でAudioContextを効果音と共有、`src/hooks/useBackgroundMusic.ts`（`themeMode`と同じApp.tsx常駐パターン）で画面遷移をまたいで再生を継続する（2026-08-06）
- [x] すうじ/順唱/空間/変化検出/音・色の5モードへ出題重み付けを追加。固定候補プールを持たないため、生成した系列パターンを粗い特徴（バケット）に分類し、バケット単位の正誤統計に基づく重み付き抽選（複数候補生成→苦手なバケットほど選ばれやすい）を導入した（`src/lib/questionWeighting.ts`、`gyaku-fukushou:questionStats:<mode>`、ことばモードの`phraseStats.ts`と同じ重み計算式、2026-08-07）
- [x] 順唱モードを廃止し、すうじモード（逆から入力/合計を入力）をホーム画面上で独立した2つのモードカードに分割（すうじタイプ選択画面を削除しカードから直接レベル選択へ。全9モード構成は維持。ランダムモードの5ラウンドもすうじ2種+空間+変化検出+音/色に変更、2026-08-07）
- [x] 統計画面の「ワーキングメモリの目安」を、一般的な心理学的基準との比較から、ユーザー自身の過去の正答率（前半/後半）との自己比較方式に変更（見出しも「ワーキングメモリの伸び」に変更、`src/lib/benchmarks.ts`、2026-08-07）
- [x] 統計画面の「モード別の正答率」を、モード×レベルの縦積みリスト（24行）からモードごとに1行へまとめたコンパクトなレイアウトに変更し、画面の縦幅を圧縮（2026-08-07）
- [x] BGMをC3/A2/F2/G2の低めの進行からC4-G3-A3-F3のI-V-vi-IV進行（明るいポップ進行、プラック音つき）に刷新し、問題を解いている間（ゲーム画面表示中）は自動的にダッキング（無音化）するように変更（`src/lib/bgm.ts`の`setBgmDucked`、2026-08-07）
- [x] 効果音・BGMの出力を`DynamicsCompressorNode`経由の共有マスターバスに変更し、スマートフォン内蔵スピーカーでも聞き取りやすいよう体感音量を底上げ（`src/lib/audioContext.ts`の`getMasterBus`、2026-08-07）
- [x] ホーム画面の「今日のミッション」カードをクリック可能にし、該当モードの選択画面へ直接遷移できるように変更（2026-08-07）
- [x] ホーム画面のヒーローカード・モードカードのビジュアルデザインを刷新し、モバイルでのフォントサイズ・パディングをレスポンシブに調整（2026-08-07）
- [x] 統計画面の「ワーキングメモリの伸び」（自己比較ベンチマーク）をDual N-Back・ランダム・ことば・音/色の4モードにも拡張し、全8モードで表示できるように変更（`src/lib/benchmarks.ts`、2026-08-08）
- [x] 実績「🌱 成長中」を追加。2つ以上のモードで「ワーキングメモリの伸び」が向上中と判定されたら解除（`src/lib/achievements.ts`、計17種類に、2026-08-08）
- [x] 「ワーキングメモリの伸び」セクションを2列グリッドでコンパクト化（対象モードが4→8に倍増したため、モード別正答率と同様の手法を適用、2026-08-08）
- [x] 統計画面の「ワーキングメモリの伸び」にすうじモード（合計を入力）を追加し、全9エリアで自己比較できるように変更（`src/lib/benchmarks.ts`の`getDigitSumBenchmark`、2026-08-08）
- [x] 実績「👑 全モードマスター」を追加。全8モードでレベル3に挑戦履歴があれば解除される集約実績（`src/lib/achievements.ts`、計18種類に、2026-08-08）
- [x] 設定画面の効果音セクションに「🔊 テスト再生」ボタンを追加（音声合成セクションと同様の即時プレビュー、2026-08-08）
- [x] ホーム画面のモードカードに「向上中」バッジ(🌱)を表示。「ワーキングメモリの伸び」で正答率が向上中のモードに、統計画面を開かなくても気づけるように（`TopScreen.tsx`、2026-08-08）
- [x] `digits.test.ts`・`pattern.test.ts`・`spatial.test.ts`・`tone.test.ts`の出題重み付け統計テストのフレーキーさを解消。5000試行の実測に基づき閾値・試行回数を調整（20回連続実行で安定確認、2026-08-08）
- [x] ホーム画面のサブタイトル・各モードカードの説明文を、全モードに即した簡潔な文言に書き直し（2026-08-08）
- [x] Nバックモードを数字ベースから3×3グリッドの位置ベース（空間版Nバック課題）に変更し、出題数を10/20/30問から選択可能に（`src/lib/nback.ts`、`NBackTrial.digit`→`position`、2026-08-08）
- [x] 学習カレンダーの視認性を改善。色の濃淡の意味のキャプション化、回数付き凡例（0/1/2/3+）、曜日・月ラベルの表示、今日のマスの強調（2026-08-08）
- [x] 統計画面の実績セクションに解除数「n / 全件数」表示を追加し、グリッドをコンパクト化。タップで詳細をインライン表示できるように変更し、プレイヤーLv関連の実績を3件追加（計21種類に、2026-08-08）
- [x] 「Dual N-Back」モードの表記を日本語UIで「デュアルNバック」に統一し、Nバックモードと同様に出題数を10/20/30問から選べるように変更（`src/lib/dualNback.ts`、2026-08-08）
- [x] 総合レビュー（実機操作+コード精査）に基づく改善20件を実施（2026-08-09）:
  - 修正10件: 通知APIのレート制限・入力検証、localStorageスキーマ検証、Cron並列化、PWAショートカット追加、StatsScreen分割、0XPフィードバック、タップ領域拡大、reminder.ts複製のCI差分チェック、バージョン同期の自動化、ホーム画面レイアウト改善
  - 機能追加10件: Nバックのアダプティブ難易度、ランダムモードの弱点重視オプション、誤答パターンの質的フィードバック、7日間チャレンジ、PWAインストール促進バナー、モード別正答率の時系列グラフ、回答フェーズの一時停止、自己ベスト強調シェア、デイリーチャレンジ、初回オンボーディングガイド（家族・コーチ向け共有ビューの代替として採用）
- [x] 2回目の総合レビューに基づく改善20件を実施（2026-08-09）:
  - 修正10件: ランダムモードの表示位置ズレ修正、回答フェーズ指示文の統一、ホーム画面サブタイトル/カード説明の簡潔化、プレイヤーLv・今日の目標のコンパクト化、「今日のおすすめ」廃止、ミッション/お題/7日間チャレンジのチップ化、アダプティブNバックの到達N値記録修正、npm audit脆弱性解消、一時停止機能のPausableAnswering共通化・E2E網羅
  - 機能追加10件: デュアルNバックのアダプティブ難易度、すうじ/空間/変化検出/音・色へのアダプティブ難易度拡張、学習履歴のCSVエクスポート、デイリーチャレンジの難易度選択、週間/月間の学習サマリー画像エクスポート、「本日の自己ベスト」比較演出、集中モード（没入UIテーマ）、チップの自動展開設定、学習カレンダーの日別ドリルダウン、実績・達成の通知センター
- [x] 今後のAndroidアプリ実装（TWA方式を推奨）を見据えた総合レビューに基づく改善26件を実施（2026-08-10）:
  - 修正13件: 連続タップ時のテキスト選択誤発生防止、プルツーリフレッシュ誤発生防止、オンボーディング「スキップ」ボタンのタップ領域拡大、Web Manifestのid/orientation/display_override追加、通知badgeアイコンの専用画像化、Wake Lock APIによる画面消灯防止、StatsScreen/SettingsScreenの遅延読み込み化、devOptions有効化、TWAデプロイ手順書化、モバイル幅E2E追加、ことばモードのAndroid対応方針文書化、TalkBack実機検証の要ROADMAP化
  - 機能追加13件: ハプティックフィードバック、Wake Lock API、assetlinks.json雛形とTWAビルド手順、Web Manifest拡充、通知badgeアイコン、オフラインインジケータ、遅延読み込み、devOptions、バックグラウンド復帰後のセッション状態復元（すうじ/空間/変化検出/音・色）、モバイル幅E2E、音声認識のAndroid対応方針明記、Google Playデータセーフティ対応表、PWAショートカットの全モード対応
- [x] ランダムモードの出題数を3/5/7問から選べるように変更（`src/lib/random.ts`のROUND_COUNT_OPTIONS、2026-08-10）
- [x] ランダムモードが単体モード画面と異なる挙動をしていた6件のバグを修正: 結果フェーズの詳細表示欠落、すうじ（合計）ラウンドの自動採点誤判定、音・色ラウンドの配色欠落、空間/変化検出ラウンドのaria-label欠落、空間ラウンドのマスサイズ不一致、レベル提案選択時の設定引き継ぎ漏れ（2026-08-10）
- [x] ランダムモードにバックグラウンド復帰後のセッション状態復元を追加（2026-08-10）
- [x] ランダムモードのすうじ（逆から入力）・空間・音/色ラウンドで、自動採点が直前の1手を反映する前の古い状態を参照し正しく回答しても不正解になる不具合を修正（`RandomGameScreen.tsx`のfinalizeAnswer、2026-08-10）
- [x] モードを途中でやめて再挑戦した際に直前の表示済み問題を除外する仕組みを全モード共通で追加（`src/lib/recentQuestions.ts`、`questionWeighting.ts`のpickWeightedCandidateにexclude対応、2026-08-10）
- [x] ランダムモードのすうじラウンドで物理キーボードの数字入力が効かなかった不具合を修正（単体のDigitGameScreenと挙動を統一、2026-08-10）
- [x] すうじ/空間/変化検出/音・色の1セット3問内で同じ問題が重複して出題されうる不具合を修正（`pick*QuestionSet`が同一セット内の既選択問題も除外するように、2026-08-10）
- [x] Nバック/デュアルNバック（最大30試行/60行）の結果画面で全試行の内訳が常時展開表示され結果画面が長大化する問題を改善。8問超で既定折りたたみ・ボタンで展開可能に（`SetSummary.tsx`、2026-08-10）
- [x] アプリ名を「逆復唱トレーニング」から「おぼえトレ」にリブランディング。公開URLを`https://oboetore.vercel.app/`に変更（Vercelプロジェクトrename）。localStorageキー接頭辞・npmパッケージ名は互換性維持のため変更していない（GitHubリポジトリ名は後日`oboetore`へrename、下記参照）（2026-08-11）
- [x] ホーム画面を9モードグリッド＋ミッション/お題/7日間チャレンジのチップ行から、[ランダムモード]/[個別選択モード]/[今日のミッション]の3ボタンにシンプル化。9モードカードグリッドは新設の「個別選択モード」画面（`ModeSelectScreen.tsx`）に移動（2026-08-11）
- [x] ホーム画面に「今日のミッション」専用画面を新設。過去の正答率から自動選定した弱点モード・レベルを1日3セット達成すると+100XP、達成後はグレーアウト表示（`src/lib/dailyMission.ts`、`DailyMissionScreen.tsx`、2026-08-11）
- [x] 統計画面に「トレーニングスコア」（総合＋数字記憶/空間記憶/注意制御の3カテゴリ、グラフ無しの前回比較表示）を追加し、既存の「正答率の推移」「モード別の正答率推移」グラフと「ワーキングメモリの伸び」ベンチマークグリッドを削除して統合（`src/lib/trainingScore.ts`、`StatsScoreSection.tsx`、2026-08-11）
- [x] 統計画面の学習カレンダーの表示期間を直近18週間から直近5週間（約1ヶ月）に短縮（2026-08-11）
- [x] 通知センターから解除・達成後3日超経過した項目を除外するように変更（2026-08-11）
- [x] 実績を2件追加（🎖️ スコアマスター・🧭 バランストレーナー、計24種類に、2026-08-11）
- [x] ランダムモードのレベル選択画面で、出題数に加えて出題するモードも個別に選べるように変更（`src/lib/random.ts`のenabledTypes、2026-08-11）
- [x] 新モード「処理記憶モード」（ops-span）を追加。簡単な暗算の正誤判定と数字の記憶を交互に繰り返す二重課題ワーキングメモリ課題（Operation Span Taskを参考）。個別選択モード・ランダムモードの両方に対応（`src/lib/opsSpan.ts`、`useOpsSpanTrialFlow.ts`、2026-08-11）
- [x] ランダムモードにことばモードのラウンドを追加。音声合成/認識の入出力を持つため、`RandomWordRound.tsx`という自己完結したコンポーネントとして統合（2026-08-11）
- [x] GitHubリポジトリ名を`gyaku-fukushou-app`から`oboetore`にリネーム（Vercelプロジェクト名と統一、2026-08-11）
- [x] ホーム画面表示時のチップ自動展開設定（`AppSettings.autoExpandChip`）を削除。付随して呼び出し元が無くなっていた`TopEngagementChips.tsx`/`DailyChallengeCard.tsx`/`src/lib/program.ts`も削除（2026-08-11）
- [x] 統計画面の学習カレンダーのマス表示を拡大し、モード別正答率をプルダウン選択式に変更して画面を圧縮（`StatsCalendarSection.tsx`/`StatsAreaAccuracySection.tsx`、2026-08-11）
