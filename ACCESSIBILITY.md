# アクセシビリティ方針

逆復唱トレーニングは音声入出力を主な操作手段とするアプリのため、音声・視覚のいずれか一方に依存しないUIを心がける。本書は現状の対応状況と、実装時に守るべき方針をまとめたもの。

## 基本方針

- **セマンティックHTML優先**: 見出し（`h1`/`h2`）、`button`、`ul`/`li`など意味のある要素を使い、装飾のためだけに`div`を乱用しない。
- **色だけに依存しない**: 正誤判定などの状態は色（緑/赤）だけでなく、テキスト（「正解」「不正解」）や記号（⭕/❌）でも伝える。
- **アイコンのみのボタンには`aria-label`を付与する**: 例）`TopScreen`の📊統計・⚙️設定ボタン、`NumpadInput`の⌫ボタン。
- **装飾的な絵文字・記号は`aria-hidden="true"`にする**: スクリーンリーダーが冗長な読み上げをしないよう、隣接するテキストラベルがある絵文字は`aria-hidden`にする。
- **動的に変化する状態は`aria-live`で通知する**: 出題フェーズの切り替わり（`reading`/`repeat`/`listening`/`result`など）は`aria-live="polite"`のコンテナ内に置く。カウントダウンの数字自体は頻繁に変化しすぎるため`aria-hidden="true"`とし、代わりに固定テキスト（「よく聞いてください」等）を読み上げ対象にする。
- **フォーカス管理**: SPA内の画面遷移時、スクリーンリーダー利用者が新しい画面に気づけるよう、`<main>`にフォーカスを移す（`src/App.tsx`の`view.screen`変化時）。
- **キーボード操作対応**: すうじモードは物理キーボードでの数字入力・Backspace・Enterに対応。Nバックモードはスペースキーで「一致」ボタンと同等の操作が可能。
- **タップ領域**: `touch-manipulation`と十分なpadding（最低44px相当を目安）でタップ操作を最適化する。
- **`lang`属性**: `index.html`の`<html lang="ja">`を維持する。

## 現状の対応（実装済み）

- 見出し・ボタン・リストなどセマンティックな要素を使用
- アイコンのみのボタンに`aria-label`（統計・設定・削除ボタン等）
- 出題フェーズのコンテナに`aria-live="polite"`、変化の激しいカウントダウン数字に`aria-hidden="true"`
- 正誤判定はテキスト＋絵文字の両方で表現（`ResultBadge`）
- 進捗ドット（`GameHeader`）は同内容のテキスト（「問題 X / Y」）と重複するため`aria-hidden="true"`
- 実績グリッドに「解除済み/未解除」のスクリーンリーダー専用テキスト（`sr-only`）を付与
- 音声認識非対応時の警告バナーに`role="status"`
- 画面遷移時のフォーカス移動（`App.tsx`）
- ダークモード対応、`focus-visible`によるフォーカスリング表示

## 既知の未対応・今後の課題

- **カラーコントラストの体系的な検証**: 現状は目視確認のみ。WCAG AA基準（通常テキストで4.5:1以上）でのコントラスト比の機械的検証は未実施。
- **音声認識・音声合成に依存する機能の代替手段**: `SpeechRecognition`非対応ブラウザでは、ことばモードのレベル選択自体を無効化している（代替入力手段は未提供）。将来的にはテキスト入力での代替回答手段の検討余地がある。
- **自動テストによるアクセシビリティ検証**: 現状はE2Eテスト（Playwright）に`axe-core`等のアクセシビリティチェックを組み込んでいない。
- **スクリーンリーダーでの実機検証**: NVDA/VoiceOver等での実際の読み上げ確認は未実施。目視・コードレビューベースの対応にとどまる。

## 関連ファイル

- [CLAUDE.md](CLAUDE.md) — アプリ全体の仕様
- [ROADMAP.md](ROADMAP.md) — 今後の開発候補
